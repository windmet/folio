# 项目需求文档 (PRD)：冷门同好“隐身”熟肉存档与分发系统

## 1. 项目概述与核心目标

### 1.1 背景与痛点

本项目用于某冷门日圈公众人物的杂志访谈文本、图片熟肉（翻译）的存档与宣发。由于圈子极小且环境敏感，项目核心原则为“极高审美、内容至上、彻底去个人IP化（隐身）”。

### 1.2 核心目标

1. **大本营（存档站）**：搭建一个零成本托管、国内访问流畅、具备日式文库本极简美学、支持段落级中日对照的高级静态网站。
2. **分发端（小红书自动化）**：编写一个本地脚本，将大本营的 Markdown 稿件自动转换为符合小红书尺寸（3:4）的高审美排版长图序列，以便无痛、匿名发布全文。

---

## 2. 技术栈架构

* **大本营前端框架**：Astro (v4.x+) — 极致的静态性能与原生 MDX 支持。
* **样式库**：Tailwind CSS — 用于响应式与极简美学样式控制。
* **托管平台**：Cloudflare Pages — 零成本、国内连通率高、支持自定义非 CDN 域名。
* **评论系统**：Giscus (基于 GitHub Discussions) — 提高评论门槛，隔绝非理性粉圈争吵。
* **分发端脚本**：Node.js + Puppeteer (或 Python + Playwright) — 负责本地无头浏览器渲染并切图。

---

## 3. 功能需求与技术实现细则

### 3.1 模块一：Astro 静态存档站

#### A. 视觉与排版规范（日式文库本风格）

* **配色**：纯白/米白背景（如 `bg-stone-50`），深灰文字（如 `text-stone-800`），保持极高留白比。
* **字体体系**：
* 中文：优先调用系统自带的高级衬线体（如 `Songti SC`, `Noto Serif CJK SC`, `SimSun` 仿宋）。
* 日文：优先调用日系明朝体（如 `Mincho`, `Hiragino Mincho ProN`）。


* **版式**：正文限制最大宽度（如 `max-w-2xl`），加大行高（`leading-relaxed` 或 `leading-loose`）与段落间距。

#### B. 核心组件：`<BilingualParagraph/>` (中日段落折叠)

* **输入参数 (Props)**：
* `cn` (string / slot): 中文翻译文本。
* `jp` (string / slot): 日语原文文本。


* **前端交互与样式**：
* 默认状态：仅显示中文文本。中文文本右侧或末尾隐藏一个轻量、极简的“原”字或图标按钮。
* 鼠标悬停/聚焦：显示该按钮。
* 点击事件：在中文下方平滑展开（Slide Down 动画）日语原文。
* 日语原文样式：字号稍小（如 `text-sm`），颜色淡化（如 `text-stone-400`），字体切换为明朝体。
* **技术限制**：使用原生 HTML5 `<details>` + `<summary>` 配合 CSS 动画，或轻量级 Vanilla JS 实现，禁止引入大型重度前端框架。



#### C. 响应式媒体控制（横竖扫图混排）

* **头图 (Hero Image)**：文章顶部统一宽屏大图，自动裁剪或按比例缩放。
* **文中插图**：
* 不允许硬编码图片宽高。
* 统一规则：`w-full h-auto max-h-[80vh] object-contain mx-auto rounded-md`。
* **竖图约束**：高度绝对不能超过当前视口的 80%，确保读者在手机或电脑上不会出现“一张竖图填满整屏还看不全”的情况。
* **横图/跨页图**：宽度自适应容器，保持原比例。



#### D. 隐私与评论安全

* 完全不出现任何个人简介、社交账号链接、关于我等页面。
* 集成 **Giscus** 评论插件：
* 限定特定 Repository。
* 读者必须登录 GitHub 账户方可评论，利用高门槛天然过滤非技术/非核心圈的碎嘴游客。



---

### 3.2 模块二：本地小红书长图自动生成脚本

#### A. 输入与渲染流

* **输入**：本地的 Markdown / MDX 熟肉文章（包含文字和图片路径）。
* **处理机制**：
1. 脚本启动本地 Chromium 无头浏览器，加载一个固定的本地 HTML 模板。
2. 将 Markdown 解析为 HTML 并注入模板。
3. 模板容器宽度固定为 `1080px`，对应小红书官方推荐分辨率。



#### B. 排版与自动分页规则

* **画布比例**：标准 3:4 比例，单张分辨率固定为 `1080px * 1440px`。
* **排版样式**：与大本营保持一致的极简优雅衬线体，四周留出至少 `80px` 的安全边距（Padding）。
* **智能分页算法**：
* 由于文章可能长达数千字，脚本需要计算内容高度。
* 每满 `1440px` 高度，自动平滑切断，并创建下一张画布。
* **防断字/断图优化**：文字不能在单行中间被横向切断（需按段落或行计算安全切断点）；图片如果刚好压在分页线上，需整体推移至下一张画布起始位置。


* **输出**：在本地指定目录输出序列图片：`page_01.png`, `page_02.png`, `page_03.png` ...（单次上限 9-10 张，符合小红书发图限制）。

---

## 4. 阶段性开发路径（供 Claude Code 执行参考）

### Phase 1: 存档站雏形搭建

1. 使用 Astro CLI 初始化项目（选择空模板，集成 Tailwind CSS）。
2. 配置 Cloudflare Pages 部署配置文件（`wrangler.toml` 或自动 Git 托管配置）。
3. 建立全局样式，配置文库本字体样式和媒体自适应 CSS。

### Phase 2: 核心组件与 MDX 跑通

1. 编写 `BilingualParagraph.astro` 组件。
2. 配置 `astro.config.mjs` 以支持 MDX。
3. 创建一篇测试 MDX 文章，验证“中日折叠切换”与“横竖图混排”的实际视觉效果。

### Phase 3: 自动化脚本开发

1. 在本地建立 `scripts/xhs-generator/` 目录。
2. 编写基于 Puppeteer 的本地渲染切图脚本。
3. 测试长文本，确保分页算法不会切断主干文字和图片。

---

> **给 AI 的提示 (Prompt Tip)**: *请严格按照上述 PRD 规范进行代码编写，优先实现 Phase 1 的静态站基建。保持代码的轻量化与高可维护性。*